# Server Port Configuration
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=github_analyzer

# GitHub Access Token (Optional but highly recommended to prevent rate limits)
# Create one at: https://github.com/settings/tokens
GITHUB_TOKEN=
